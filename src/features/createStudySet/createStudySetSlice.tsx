import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  studySet: {}
}

export const createStudySetSlice = createSlice({
  name: 'createStudySet',
  initialState,
  reducers: {
    
  }
})